# SBR DIGITAL Logging to JSON

![Python Version](https://img.shields.io/badge/python-3.12%2B-3776AB?logo=python&logoColor=white)
![License: MIT](https://img.shields.io/badge/license-MIT-success)
![Log Format](https://img.shields.io/badge/logs-NDJSON-orange)
![Dependencies](https://img.shields.io/badge/deps-none-brightgreen)

## Навигация
- [📚 Обзор](#обзор)
- [✨ Возможности](#возможности)
- [🔌 Встраивание](#встраивание-в-проект)
- [🚀 Быстрый старт](#быстрый-старт)
- [🧱 Архитектура](#архитектура)
- [🧪 Тесты](#тесты)
- [📄 Лицензия](#лицензия)

## Обзор

`python_json_logger` — модуль поверх стандартного `logging` для:
- текстового логирования с дневной ротацией файлов;  
- структурированного логирования в формате NDJSON (`.ndjson`);  
- чтения JSON-логов обратно в Python-объекты.

Модуль не привязан к конкретному фреймворку и может использоваться в
любой прикладной логике (сервисы, боты, CLI-инструменты и т.п.).

## Возможности

- `AppLogger` — файловый логгер с ежедневной ротацией (`DD_MM_YY_logs.log`).
- `JsonAppLogger` — JSON-логгер в формате NDJSON (`DD_MM_YY_logs.ndjson`).
- Единый интерфейс для получения логгеров:
  - `get_app_logger(name, level, to_console=False)`;
  - `get_json_app_logger(name, level, to_console=False)`.
- Уровни логирования через тип `LogLevels` (`"DEBUG" | "INFO" | ...`).
- Чтение JSON-логов:
  - `JsonLogReader` — утилита для построчного чтения NDJSON;
  - `JsonLogRecord` — dataclass с полями записи (включая данные об исключениях).
- Ежедневная ротация файлов логов без сторонних зависимостей.
- Готовые тестовые сценарии с кастомными исключениями, демонстрирующие
  работу логирования ошибок и стек-трейсов.

## Клонирование репозитория

Исходный код доступен на GitHub:

https://github.com/SBRDIGITAL/python_json_logger

Клонировать репозиторий (HTTPS):

```bash
git clone https://github.com/SBRDIGITAL/python_json_logger.git
cd python_json_logger
```

Или через SSH (если настроен ключ):

```bash
git clone git@github.com:SBRDIGITAL/python_json_logger.git
cd python_json_logger
```

## Встраивание в проект

### Установка

Проект зависит только от стандартной библиотеки Python.  
Код написан на Python `3.12.9`.

В проект модуль можно добавить как подпакет (git submodule, mono-repo
и т.п.) или вынести в отдельный пакет и установить через `pip`.

Пример локального подключения как подпакета (структура репозитория):

```text
your_project/
	app/                # Ваше приложение
	logging_to_json/    # Этот модуль (склонирован внутрь репозитория)
```

После этого в вашем коде можно просто импортировать публичный интерфейс:

```python
from app.modules.logging import (
    get_app_logger,
    get_json_app_logger,
    JsonLogReader,
    JsonLogRecord,
)
```

### Базовое использование

```python
from app.modules.logging import get_app_logger, get_json_app_logger

logger = get_app_logger(__name__, level="INFO", to_console=True)
json_logger = get_json_app_logger(__name__, level="INFO")

logger.info("Сервис запущен")

try:
    1 / 0
except ZeroDivisionError:
    # В текстовый лог и в NDJSON попадут поля об исключении
    logger.exception("Ошибка деления")
    json_logger.exception("Ошибка деления")
```

Логи будут сохранены в `logs/<имя_логгера>/DD_MM_YY_logs.log` и
`logs/<имя_логгера>/DD_MM_YY_logs.ndjson`.

### Чтение JSON-логов

```python
from pathlib import Path
from app.modules.logging import JsonLogReader

log_file = Path("logs") / "my_service" / "07_12_25_logs.ndjson"
reader = JsonLogReader(log_file)
records = reader.to_records()  # list[JsonLogRecord]

for record in records:
    print(record.time, record.level, record.message)
```

## Быстрый старт

### Подготовка окружения

Пример для Windows + bash:

```bash
cd loggin_to_json
python -m venv .venv
source .venv/Scripts/activate
python -m pip install --upgrade pip
```

### Запуск демонстрации

В репозитории есть простая демо-программа `main.py`, которая показывает,
как работают оба логгера и `JsonLogReader`:

```bash
cd loggin_to_json
python main.py
```

Скрипт:

- создаёт текстовый и JSON-логгеры;
- логирует диапазон чисел `0..10`;
- запускает сценарии с исключениями из `app.tests.scenarios`;
- рекурсивно находит все `*.ndjson` в каталоге `logs`, читает их через
  `JsonLogReader` и выводит в консоль:
  - список найденных файлов;
  - общее количество объектов `JsonLogRecord`;
  - пример списка записей.

После запуска в каталоге `logs/` появятся подпапки
`__main__/`, `app.tests.scenarios/` с файлами вида
`DD_MM_YY_logs.ndjson` и `.log`.

## Архитектура

### Структура каталогов

```text
loggin_to_json/
├─ main.py                         # Точка входа, демонстрация
├─ app/
│  ├─ __init__.py                  # Корневой пакет
│  ├─ modules/
│  │  ├─ __init__.py              # Пакет модулей
│  │  └─ logging/
│  │     ├─ __init__.py           # Публичный интерфейс подсистемы логгирования
│  │     ├─ internal.py           # Определение типа LogLevels
│  │     ├─ text/
│  │     │  ├─ __init__.py        # Реэкспорт AppLogger и get_app_logger
│  │     │  └─ app_logger.py      # Текстовый логгер с ротацией
│  │     └─ json/
│  │        ├─ __init__.py        # Реэкспорт JSON-логгера и ридера
│  │        ├─ json_logger.py     # JsonAppLogger и DailyJsonRotatingFileHandler
│  │        └─ json_reader.py     # JsonLogReader и JsonLogRecord
│  └─ tests/
│     ├─ __init__.py
│     ├─ exceptions.py            # Кастомные исключения (AppError и т.п.)
│     ├─ scenarios.py             # Функции process_numbers и demo_nested_exceptions
│     └─ test_json_logs.py        # Тесты для JSON-логгера и чтения логов
└─ logs/
   └─ ...                         # Автоматически создаваемые директории и файлы логов
```

### Основные компоненты
- `AppLogger` (`app.modules.logging.text.app_logger.AppLogger`)
  - наследник `logging.Logger` с заранее настроенными обработчиками;
  - пишет в `logs/<logger_name>/DD_MM_YY_logs.log` через
    `DailyRotatingFileHandler`.
- `JsonAppLogger` (`app.modules.logging.json.json_logger.JsonAppLogger`)
  - пишет каждую запись как отдельный JSON-объект (NDJSON);
  - использует `DailyJsonRotatingFileHandler` для ротации файлов.
- `JsonLogReader` (`app.modules.logging.json.json_reader.JsonLogReader`)
  - читает `.ndjson`-файлы построчно;
  - конвертирует каждую строку в словарь или в dataclass
    `JsonLogRecord`.
- `JsonLogRecord` (`app.modules.logging.json.json_reader.JsonLogRecord`)
  - неизменяемый dataclass, описывающий одну запись лога;
  - содержит как основные поля (`time`, `level`, `message`), так и
    информацию об исключении (`exc_type`, `exc_message`, `exc_traceback`).

## Тесты
Тесты находятся в пакете `app.tests` и покрывают работу JSON-логгера и
чтение логов.

Запуск тестов (если установлен `pytest`):

```bash
cd loggin_to_json
pytest
```

Основные проверки в `app/tests/test_json_logs.py`:

- корректная запись информации об исключениях в NDJSON-файл;
- наличие CRITICAL-записи для вложенной ошибки `AppError`;
- корректное чтение NDJSON через `JsonLogReader` и получение списка
  объектов `JsonLogRecord`.

## Лицензия
- Проект распространяется под лицензией MIT.
- Полный текст лицензии см. в файле [`LICENSE`](./LICENSE).
